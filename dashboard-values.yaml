protocolHttp: "true"

#service:
#  externalPort: 80

#extraArgs:
#  - --enable-insecure-login

annotations:
  konghq.com/protocol: "https"

ingress:
  enabled: "true"
  annotations:
    kubernetes.io/ingress.class: kong
    konghq.com/preserve-host: 'true'
  hosts:
    - dashboard.kubernetes.infiquetra.com
